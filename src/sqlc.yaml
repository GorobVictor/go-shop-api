version: "2"
servers:
- engine: postgresql
  uri: "postgres://shop_user:shop_password@localhost:5432/shop_db?sslmode=disable"
sql:
  - engine: "postgresql"
    queries: "proto/db/queries"
    schema: "proto/db/schemas"
    gen:
      go:
        out: "generated/db"
        sql_package: "pgx/v5"